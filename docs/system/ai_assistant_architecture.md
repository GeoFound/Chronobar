# Chronobar AI Assistant 架构说明 v1.0

## 1. 文档定位

本文档定义 Chronobar AI Assistant 的产品级系统骨架，描述分层模型、运行链路、模块职责、依赖约束与演进边界。
它约束的是 Chronobar 产品内部面向产品使用者的 **产品 AI**，不定义仓库设计、文档维护、代码生成、迁移与重构过程中使用的 **仓库 AI**。

它不替代 [`../core/ai_protocol.md`](../core/ai_protocol.md) 中对 AI 插件的协议定义，而是补充“Chronobar 如何把受控 AI 插件、检索能力、工具能力、治理能力和评测能力组织成一个可落地助手系统”。

本文档描述的是 M2 及之后的 AI Assistant 实施蓝图；M1 阶段只冻结边界和契约，不假定这些运行时模块已经全部实现。

## 2. 目标与非目标

### 2.1 目标

1. 让 AI 成为受控的交易助手，而不是自由执行的自治代理。
2. 让文档问答、回测解读、风控解释、配置解释共享统一编排骨架。
3. 让回答尽量建立在系统数据或正式文档证据之上，而不是纯生成。
4. 让风险边界、权限控制、来源保留、调用审计与评测成为一等能力。
5. 让后续 MCP 接入、多模型调度、混合工作流扩展有稳定演进路径。
6. 让 AI 可以把用户意图落实为可验证、可确认、可应用的候选正式产物，而不是只停留在一次性聊天草稿。

### 2.2 非目标

- 不将 AI Assistant 设计为自动下单代理。
- 不允许 AI 绕过 [`ui_bridge_protocol.md`](ui_bridge_protocol.md) 或 Host API 直接访问核心私有对象。
- 不将模型输出视为事实真相源。
- 不在 M1 阶段引入大规模运行时实现。

## 3. 设计原则

- **受控优先**：AI 只能在明确边界内读取、解释、建议，不能越权执行。
- **产品侧限定**：本文档中的 AI 仅指产品内提供给产品使用者的助手能力，不包含仓库侧 AI 治理、开发协作或维护迁移助手。
- **证据优先**：优先引用系统数据、正式文档或受控外部来源。
- **路由优先**：先识别问题类型，再决定检索、工具调用、拒答或回答。
- **分级动作**：解释类动作可直接回答，修改类动作必须确认，执行类动作默认禁用。
- **产物可用**：修改类输出应优先生成最小可用候选产物，并附校验结果。
- **可审计**：问题、路由、工具调用、来源、模型版本、回答结果必须可记录。
- **可评测**：产品问答、回测解读、风控解释、拒答等任务必须独立评估。

## 4. 九层模型

Chronobar AI Assistant 采用九层模型，用于把“聊天能力”提升为“受控交易助手系统”。

### 4.1 Client 层

职责：
- 提供聊天面板、侧边 Copilot、页内问答入口。
- 在回测页、风控页、配置页、帮助页内承载任务型交互。
- 展示来源、置信提示、拒答说明、确认按钮和操作回执。

约束：
- 不直接访问模型或底层工具。
- 所有请求必须通过受控桥接边界进入编排层。

### 4.2 State / Storage 层

职责：
- 保存短期会话状态。
- 保存当前页面、当前对象、最近问题、最近证据、最近工具调用结果。
- 为 AI Assistant 提供可清理、可过期的任务上下文。
- 在用户授权范围内保存用户偏好、常用术语和工作区级记忆。

约束：
- 默认只保留短期、受控、可删除状态。
- 长期记忆必须显式 opt-in，并区分会话记忆、工作区记忆和用户记忆。
- 记忆服务不得绕过用户管理边界直接持久化敏感策略内容。

### 4.3 Orchestrator 层

职责：
- 识别请求类型。
- 决定走文档检索、结构化工具查询、拒答、澄清还是回答。
- 管理降级、重试、确认点和失败回退路径。

约束：
- 不能把所有问题无差别丢给 LLM。
- 必须先过策略检查，再进入工具或检索阶段。

### 4.4 Tool 层

职责：
- 对接系统内结构化能力。
- 支持查询回测结果、配置、工作区、风险事件、日志、订单、持仓等。
- 支持生成候选配置、候选代码、候选工作区对象以及相应的最小校验结果。

约束：
- 所有工具必须有稳定输入输出结构。
- 工具调用必须受权限矩阵和策略引擎限制。
- 修改类工具不得直接覆盖正式对象，必须输出候选产物与待确认变更。

### 4.5 Skill / Catalog 层

职责：
- 管理可发现能力单元。
- 为不同问题类型绑定 skill、prompt 模板、工具集合、适用页面和权限声明。

约束：
- skill 不是自由脚本入口，而是受注册表和策略控制的能力目录。

### 4.6 Semantic Index 层

职责：
- 为 README、协议文档、FAQ、帮助文档和错误说明提供语义检索。
- 将关键词搜索升级为证据检索。

约束：
- 仅索引正式受控知识源。
- 检索结果必须保留来源位置。

### 4.7 MCP / External Context 层

职责：
- 以标准化方式接入受控外部上下文。
- 支持交易所说明、规则库、内部知识服务、受控搜索等。
- 为期货相关问题提供真实世界可访问能力，但保持来源保留与用户可见性。

约束：
- 外部上下文默认关闭，按来源白名单接入。
- 必须明确标记为外部来源。
- 外部访问策略必须能区分一次性授权、会话授权与全局关闭状态。

### 4.8 RAI / Governance 层

职责：
- 管理边界、权限、拒答、确认点、来源保留、调用链审计与模型使用规范。
- 保证 AI Assistant 满足金融敏感场景下的风险控制要求。

约束：
- 不提供投资建议。
- 不允许执行交易。
- 不允许无证据强答。

### 4.9 Evaluation 层

职责：
- 评估产品问答、回测解读、风控解释、配置解释和拒答表现。
- 跟踪正确率、引用率、误导率、拒答准确率等指标。

约束：
- 评测必须任务分层，不能只看单一主观分数。

## 5. 最小运行链路

Chronobar AI Assistant 的推荐最小运行链路如下：

```text
Client
-> Intent Router
-> Policy Engine
-> Retriever / Tool Executor
-> Candidate Validator / Change Set Builder
-> Evidence Binder
-> Answer Composer
-> Audit Logger
```

### 5.1 Intent Router

职责：
- 识别以下问题类型：
  - 产品问答
  - 回测解读
  - 风控解释
  - 配置解释
  - 概念说明
  - 范围外问题

输出：
- `intent_type`
- `confidence`
- `required_capabilities`
- `risk_level`

### 5.2 Policy Engine

职责：
- 判断请求是否允许回答。
- 判断是否允许调用某类工具或外部上下文。
- 判断是否需要二次确认。
- 判断是否必须拒答或要求澄清。

### 5.3 Retriever / Tool Executor

职责：
- 文档问题优先走知识检索。
- 数据问题优先走结构化工具查询。
- 混合问题允许“先工具、再检索、后解释”。

### 5.4 Candidate Validator / Change Set Builder

职责：
- 对配置、代码、工作区等候选产物执行最小可用校验。
- 生成可供用户确认的 change set、影响范围摘要和应用前回执。
- 保证修改类输出不是黑箱文本，而是可审查、可应用的候选对象。

### 5.5 Evidence Binder

职责：
- 绑定文档证据、系统数据证据和外部证据。
- 形成可被 Answer Composer 使用的统一证据包。

### 5.6 Answer Composer

职责：
- 生成最终回答。
- 显示依据来源、动作级别、不确定性和下一步建议。

### 5.7 Audit Logger

职责：
- 记录问题、会话、路由、调用工具、证据来源、模型版本和回答结果。

## 6. Chronobar 最小能力面

M2+ 建议按风险分层逐步落地以下高价值能力面：

1. **产品问答 Copilot**
   - 回答协议、配置、错误码、功能说明。
2. **回测解读 Copilot**
   - 解释收益、回撤、胜率、滑点、异常区间。
3. **风控解释 Copilot**
   - 解释命中的规则、拦截原因和修复建议。
4. **用户资产构建 Copilot**
   - 生成配置、工作区、指标或策略候选产物，并附带最小校验结果与待确认变更。
5. **真实世界研究 Copilot**
   - 在白名单外部来源范围内补充交易所规则、公告和合约资料。
6. **多因子研究 Copilot**
   - 辅助生成因子假设、特征工程方案、实验配置、结果比较与研究候选产物，但不直接把研究结论发布为运行态对象。

这些能力共用同一套编排骨架，但可绑定不同 skill、工具与策略配置。

## 7. 状态模型

AI Assistant 只应默认使用受控的短期会话状态，至少包括：

- 当前页面
- 当前工作区
- 当前合约
- 当前回测任务
- 最近一次问题
- 最近一次工具调用结果
- 最近一次证据包摘要

在用户授权后，可逐步增加：

- 工作区级偏好记忆
- 用户级术语与工作流偏好记忆
- 最近确认过的候选产物上下文

不建议在第一阶段引入默认开启、不可见、不可管理的长期记忆。

## 8. 工具能力边界

AI Assistant 工具分为以下几类：

- **知识工具**：查询正式文档、FAQ、错误码、帮助中心
- **系统查询工具**：查询回测结果、工作区、配置、插件、日志、风险事件
- **研究工具**：查询因子定义、实验配置、评估结果、研究报告与发布状态
- **受控外部工具**：查询受白名单限制的外部文档或规则说明
- **修改类工具**：生成配置候选产物、生成代码候选产物、参数调整建议
- **治理类工具**：候选产物校验、change set 构建、记忆查看/清除、确认状态检查

约束：
- 修改类工具必须走确认流程。
- 执行型工具默认不纳入 AI Assistant 范围。
- 研究类工具应优先服务于构建态，不得把未确认研究结论直接注入运行态。
- 治理类工具必须为用户提供来源、校验和可撤销边界。

## 9. 与现有协议的关系

### 9.1 与 AI 插件协议的关系

- [`../core/ai_protocol.md`](../core/ai_protocol.md) 定义 AI 插件自身的接入与权限。
- 本文档定义 AI Assistant 作为“系统级助手”的编排结构。
- AI Assistant 可消费 AI 插件能力，但不被 AI 插件协议替代。

### 9.2 与 UI Bridge 协议的关系

- [`ui_bridge_protocol.md`](ui_bridge_protocol.md) 仍然是前端访问核心能力的唯一正式边界。
- AI Assistant 的 Client 层与 Tool 层必须通过受控桥接边界访问核心能力。

### 9.3 与系统架构文档的关系

- [`architecture.md`](architecture.md) 定义 Chronobar 主体七层架构。
- 本文档在其上补充 AI Assistant 的产品级辅助层与治理层，不改变主体依赖方向。

### 9.4 与仓库 AI 治理文档的关系

- `../../ai-instructions.md` 与 `../engineering/engineering_baseline.md` 约束的是仓库 AI 的设计、实现、测试、维护与迁移协作行为。
- 本文档不定义仓库 AI 的权限、记忆、迁移或重构职责，也不把仓库 AI 的行为直接暴露为产品能力。
- 若未来仓库 AI 与产品 AI 共享底层模型或基础设施，两者仍必须保持身份、权限、记忆与审计边界分离。

## 10. 依赖约束

允许依赖：

- Client -> Orchestrator
- Orchestrator -> Policy Engine / Catalog / Retriever / Tool Executor
- Retriever -> Semantic Index / 受控知识源
- Tool Executor -> UiBridge / Host API / 标准查询接口
- Answer Composer -> 证据包 / 模型接口
- Audit Logger -> 标准审计存储

禁止依赖：

- Client -> 核心模块私有对象
- LLM -> 直接访问数据库连接或 Gateway 实例
- 外部上下文 -> 绕过策略引擎直接进入回答阶段
- 修改类工具 -> 绕过确认流程直接执行
- 任意组件 -> 绕过审计直接调用高风险能力

## 11. 治理要求

Chronobar AI Assistant 至少必须满足以下治理要求：

- 明确声明不提供投资建议。
- 明确区分系统数据、正式文档、外部来源和模型推断。
- 对高风险或高不确定性回答显示不确定性提示。
- 对修改类动作要求明确确认。
- 对范围外问题、越权请求、缺少证据问题执行稳定拒答。
- 对所有关键路径保留审计记录。
- 对记忆启用、外部访问启用和候选产物应用保留单独审计记录。

## 12. 评测要求

至少建立以下任务级评测线：

- 产品问答正确率
- 回测解读一致率
- 风控解释准确率
- 配置解释正确率
- 来源引用命中率
- 拒答准确率
- 误导率
- 候选产物应用成功率
- 外部上下文引用准确率
- 记忆命中后的人工纠错率
- 研究配置复跑成功率
- 因子/实验结果解释一致率

评测结果应服务于路由规则、提示模板、工具优先级和来源策略的持续改进。

## 13. 演进路线

### 13.1 Phase 1

- 产品问答 Copilot
- 回测解读 Copilot
- 风控解释 Copilot

### 13.2 Phase 2

- 概念解释
- 受控搜索
- Session State
- 工作区记忆与用户记忆管理
- 来源展示与引用规范

### 13.3 Phase 3

- Skill Catalog
- Policy Center
- Evaluation Dashboard
- 研究 Copilot
- 用户资产构建与确认应用链路
- 多因子研究编排与研究资产发布边界

### 13.4 Phase 4

- MCP Servers
- 多工具编排
- 多模型任务分配
- deterministic / dynamic / hybrid flow 并存

## 14. 相关文档

- [`architecture.md`](architecture.md)：Chronobar 主体架构
- [`ui_bridge_protocol.md`](ui_bridge_protocol.md)：前端受控访问边界
- [`config_protocol.md`](config_protocol.md)：AI 相关配置组织与校验
- [`../core/ai_protocol.md`](../core/ai_protocol.md)：AI 插件协议与权限边界
- [`../core/plugin_protocol.md`](../core/plugin_protocol.md)：插件生命周期与 Host API
